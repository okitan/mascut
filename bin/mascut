#!/bin/env ruby
begin
  require 'rack'
rescue LoadError
  require 'rubygems'
  require 'rack'
end
require 'mascut'

app = Rack::Builder.new do
  use Rack::ShowExceptions
  run Mascut.new
end.to_app

Rack::Handler::Mongrel.run app, :Host => '0.0.0.0', :Port => 9203
